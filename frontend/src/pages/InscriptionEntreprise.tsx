import React from 'react';

/**
 * Page placeholder — EPIC 2 — STORY 2.5 (acquisition)
 * Cette page sera complétée plus tard (EPIC 4), mais elle permet déjà
 * de valider le routage et l’orientation depuis la landing.
 */
export default function InscriptionEntreprise(): JSX.Element {
  return (
    <div className="min-h-screen bg-white text-slate-900">
      <header className="p-4 border-b">Vectura — Inscription entreprise</header>
      <main className="p-4 max-w-2xl mx-auto">
        <h1 className="text-2xl font-extrabold">Inscription entreprise (placeholder)</h1>
        <p className="mt-3 text-slate-600">
          Cette page sera implémentée lors de l’EPIC 4 (Espace Entreprise).
        </p>
      </main>
    </div>
  );
}

