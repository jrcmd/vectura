# Vectura backend (squelette)

- Express + TypeScript (config minimale)
- Prisma schema initial

Prochaines étapes : installer les deps dans `backend/`, lancer `prisma migrate dev`, puis implémenter l’API.

